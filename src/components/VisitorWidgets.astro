---
---
<div class="widgets-wrapper">
    <!-- 访客计数器 (基于不蒜子) -->
    <div class="widget-card counter-card">
        <h3>访客统计</h3>
        <div class="counter-content">
            <div class="count-item">
                <span class="label">本站总访问量</span>
                <span id="busuanzi_value_site_pv" class="count-num"><i class="loading">...</i></span>
            </div>
            <div class="count-item">
                <span class="label">你是第</span>
                <span id="busuanzi_value_site_uv" class="count-num"><i class="loading">...</i></span>
                <span class="label">位小伙伴</span>
            </div>
        </div>
    </div>

    <!-- 访客地图 (MapMyVisitors / ClustrMaps / RevolverMaps) -->
    <div class="widget-card map-card">
        <h3>访客分布</h3>
        <div class="map-container">
            <!-- 
                 ℹ️ 配置指南:
                 1. 前往 https://mapmyvisitors.com/ 或 https://clustrmaps.com/ 注册你的网站
                 2. 获取 "Javascript" 或 "Image Embed" 代码
                 3. 直接粘贴在下方替换掉示例代码
            -->
            
            <!-- ⬇️ 在这里放入你的地图代码 (示例为 ClustrMaps 的占位图) ⬇️ -->
            <script type="text/javascript" id="mapmyvisitors" src="//mapmyvisitors.com/map.js?d=GfWaagulw5y8ePx2r-A54yQmg5UIATp6pJDDcwAta44&cl=ffffff&w=a"></script>
             
            <!-- 如果脚本加载失败显示的备用内容 -->
            <div class="map-placeholder" style="display:none;">
                <p>地图加载中...</p>
            </div>
        </div>
    </div>
</div>

<!-- 引入不蒜子脚本 -->
<script async src="//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js"></script>

<style>
    .widgets-wrapper {
        display: flex;
        flex-wrap: wrap;
        gap: 2rem;
        margin-top: 3rem;
        justify-content: center;
    }

    .widget-card {
        background: var(--card-bg); /* Use var */
        border: 1px solid var(--border-color); /* Use global var */
        border-radius: var(--radius); /* Use global var */
        padding: 1.5rem;
        box-shadow: var(--box-shadow);
        flex: 1;
        min-width: 300px;
        text-align: center;
        transition: all 0.3s ease;
    }
        background: var(--card-bg); /* Use var */
        border: 1px solid var(--border-color); /* Use global var */
        border-radius: var(--radius); /* Use global var */
        padding: 1.5rem;
        box-shadow: var(--box-shadow);
        flex: 1;
        min-width: 300px;
        text-align: center;
        transition: all 0.3s ease;
    }
    
    /* Override global theme styles for modern (remove top border) */
    :global(html[data-theme='modern']) .widget-card {
        border: 1px solid var(--border-color) !important;
        background-color: #fff !important;
        box-shadow: none !important;
        border-radius: 0 !important;
    }

    /* Dark mode override */
    :global(html[data-theme='nord']) .widget-card {
        background-color: var(--card-bg) !important;
        border: 1px solid var(--border-color) !important;
        box-shadow: var(--box-shadow) !important;
        color: var(--text-color) !important;
        border-radius: 8px !important;
    }

    .widget-card h3 {
        margin-top: 0;
        margin-bottom: 1rem;
        font-size: 1.2rem;
        color: rgb(var(--black));
        border-bottom: 2px solid rgb(var(--accent));
        display: inline-block;
        padding-bottom: 0.5rem;
    }

    .counter-content {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        padding: 1rem 0;
    }

    .count-item {
        font-size: 1rem;
        color: rgb(var(--gray));
    }

    .count-num {
        font-family: 'Courier New', Courier, monospace;
        font-weight: bold;
        font-size: 1.5rem;
        color: rgb(var(--accent));
        margin: 0 0.5rem;
    }

    .loading {
        font-size: 0.8rem;
        opacity: 0.5;
        font-style: normal;
    }

    .map-container {
        min-height: 200px;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        position: relative;
    }

    /* ClustrMaps 具体样式修正 */
    .map-container :global(#clustrmaps-widget-v2) {
         margin: 0 auto;
    }
</style>
